/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        serverActions: true
    },
    images: {
        domains: ['whvggfakotwestufeyys.supabase.co'],
    }
}
module.exports = nextConfig